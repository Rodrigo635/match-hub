export const metadata = {
  title: 'Contato - MATCH HUB',
  description: 'Entre em contato conosco para tirar suas dúvidas ou sugestões.',
};

export default function ContatoLayout({ children }) {
  return <>{children}</>;
}
